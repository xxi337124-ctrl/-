# 🎨 统一组件库使用指南

## 📋 概述

本组件库为内容工厂项目提供统一的UI组件，与**智能创作中心**的设计语言保持一致。所有组件都经过优化，支持动画、响应式布局和TypeScript类型安全。

---

## 📦 组件分类

### 1. Card 组件系列

位于 `src/components/common/Card/`

#### BaseCard - 基础卡片

最底层的卡片组件，提供统一的样式和动画。

```tsx
import { BaseCard } from '@/components/common/Card';

<BaseCard
  variant="gradient"
  gradientFrom="from-purple-400"
  gradientTo="to-pink-500"
  hoverEffect={true}
  clickable={true}
>
  <p>卡片内容</p>
</BaseCard>
```

**Props:**
- `variant`: 卡片变体 - `default` | `gradient` | `bordered` | `elevated`
- `hoverEffect`: 是否启用悬停效果（默认 true）
- `clickable`: 是否可点击（默认 false）
- `gradientFrom`: 渐变起始颜色
- `gradientTo`: 渐变结束颜色

#### ContentCard - 内容卡片

用于展示内容的完整卡片，包含标题、描述、标签、时间戳等。

```tsx
import { ContentCard } from '@/components/common/Card';

<ContentCard
  title="精品咖啡趋势"
  description="手冲咖啡和冷萃咖啡在年轻人中越来越受欢迎"
  icon="☕"
  badge="上升"
  badgeColor="bg-green-100 text-green-600"
  tags={['手冲', '冷萃', '精品咖啡']}
  timestamp={new Date()}
  actions={
    <>
      <button>编辑</button>
      <button>删除</button>
    </>
  }
  variant="elevated"
  hoverEffect={true}
  clickable={true}
/>
```

**Props:**
- `title`: 标题（必填）
- `description`: 描述文字
- `icon`: 图标（ReactNode）
- `badge`: 徽章文字
- `badgeColor`: 徽章颜色类名
- `tags`: 标签数组
- `timestamp`: 时间戳
- `actions`: 操作按钮
- `footer`: 自定义页脚
- `children`: 自定义内容

#### StatCard - 数据卡片

用于展示统计数据的卡片组件。

```tsx
import { StatCard } from '@/components/common/Card';

<StatCard
  title="总创作数"
  value={1234}
  icon={<FiTrendingUp />}
  trend={{ value: 12.5, isPositive: true }}
  description="较上周增长"
  color="purple"
/>
```

**Props:**
- `title`: 数据标题
- `value`: 数据值
- `icon`: 图标
- `trend`: 趋势信息 `{ value: number, isPositive: boolean }`
- `description`: 描述文字
- `color`: 颜色主题 - `blue` | `purple` | `green` | `orange` | `pink`

---

### 2. Layout 组件系列

位于 `src/components/common/Layout/`

#### PageContainer - 页面容器

用于包裹整个页面，提供统一的布局和背景。

```tsx
import { PageContainer } from '@/components/common/Layout';

<PageContainer
  title="历史记录"
  description="查看所有创作历史和分析记录"
  actions={
    <button className="px-4 py-2 bg-purple-500 text-white rounded-lg">
      新建创作
    </button>
  }
>
  {/* 页面内容 */}
</PageContainer>
```

**Props:**
- `title`: 页面标题
- `description`: 页面描述
- `actions`: 页面操作按钮
- `children`: 页面内容

#### GridLayout - 网格布局

响应式网格布局组件。

```tsx
import { GridLayout } from '@/components/common/Layout';

<GridLayout columns={3} gap={6}>
  <ContentCard title="卡片1" />
  <ContentCard title="卡片2" />
  <ContentCard title="卡片3" />
</GridLayout>
```

**Props:**
- `columns`: 列数 - `1` | `2` | `3` | `4`（自动响应式）
- `gap`: 间距 - `4` | `6` | `8`
- `children`: 子元素

**响应式规则:**
- `columns={3}`: 移动端1列，平板2列，桌面3列
- `columns={4}`: 移动端1列，平板2列，桌面3列，超大屏4列

#### Section - 区块组件

用于划分页面区块。

```tsx
import { Section } from '@/components/common/Layout';

<Section
  title="热门洞察"
  description="基于数据分析的热门内容推荐"
  actions={<button>查看全部</button>}
>
  <GridLayout columns={3}>
    {/* 内容 */}
  </GridLayout>
</Section>
```

**Props:**
- `title`: 区块标题
- `description`: 区块描述
- `actions`: 区块操作按钮
- `children`: 区块内容

---

### 3. 设计令牌 (Design Tokens)

位于 `src/lib/design/tokens.ts`

提供统一的设计变量，确保整个应用的视觉一致性。

```tsx
import { colors, spacing, animations } from '@/lib/design';

// 使用渐变色
<div className={`bg-gradient-to-br ${colors.gradients.purple}`}>
  紫色渐变背景
</div>

// 使用状态颜色
<span className={colors.status.success}>成功状态</span>

// 使用趋势颜色
<span className={colors.trend.up.color}>
  {colors.trend.up.icon} 上升趋势
</span>
```

**可用令牌:**

#### 颜色 (colors)
```typescript
colors.gradients.purple  // 'from-purple-400 to-pink-500'
colors.gradients.blue    // 'from-blue-400 to-cyan-500'
colors.status.success    // 'text-green-600 bg-green-50'
colors.trend.up          // { icon: '📈', color: 'text-green-600', bgColor: 'bg-green-100' }
```

#### 间距 (spacing)
```typescript
spacing.section    // 'mb-8'
spacing.card       // 'p-6'
spacing.gap.md     // 'gap-4'
```

#### 排版 (typography)
```typescript
typography.pageTitle      // 页面标题样式
typography.sectionTitle   // 区块标题样式
typography.cardTitle      // 卡片标题样式
```

---

### 4. 动画工具 (Animations)

位于 `src/lib/design/animations.ts`

提供与智能创作中心一致的动画效果。

```tsx
import { motion } from 'framer-motion';
import {
  cardEntrance,
  hoverEffect,
  staggerContainer,
  staggerItem
} from '@/lib/design';

// 单个卡片入场动画
<motion.div {...cardEntrance(0.2)}>
  内容
</motion.div>

// 交错动画列表
<motion.div variants={staggerContainer} initial="hidden" animate="show">
  {items.map((item, i) => (
    <motion.div key={i} variants={staggerItem}>
      {item}
    </motion.div>
  ))}
</motion.div>

// 悬停效果
<motion.div whileHover={hoverEffect(4)}>
  鼠标悬停时上移4px
</motion.div>
```

**可用动画:**

```typescript
// 卡片动画
cardEntrance(delay)      // 卡片入场动画
hoverEffect(lift)        // 悬停上浮效果
tapEffect                // 点击效果

// 容器动画
staggerContainer         // 交错容器
staggerItem             // 交错子项

// 基础动画
fadeInOut               // 淡入淡出
slideInUp               // 从下滑入
slideInRight            // 从右滑入
scaleInOut              // 缩放动画

// 特效动画
spinAnimation           // 旋转动画（加载）
pulseAnimation          // 脉动动画
shakeAnimation          // 摇摆动画
progressAnimation(n)    // 进度条动画
```

---

## 🎨 完整示例

### 示例1: 历史记录页面

```tsx
import { PageContainer, GridLayout, Section } from '@/components/common/Layout';
import { ContentCard } from '@/components/common/Card';
import { colors } from '@/lib/design';

export default function HistoryPage() {
  return (
    <PageContainer
      title="历史记录"
      description="查看所有创作历史和分析记录"
      actions={
        <button className={`px-4 py-2 bg-gradient-to-r ${colors.gradients.purple} text-white rounded-lg`}>
          新建创作
        </button>
      }
    >
      <Section title="最近创作" description="最近7天的创作记录">
        <GridLayout columns={3} gap={6}>
          {recentItems.map((item) => (
            <ContentCard
              key={item.id}
              title={item.title}
              description={item.description}
              icon="📝"
              badge={item.status}
              badgeColor="bg-green-100 text-green-600"
              tags={item.tags}
              timestamp={item.createdAt}
              variant="elevated"
              hoverEffect={true}
              clickable={true}
            />
          ))}
        </GridLayout>
      </Section>
    </PageContainer>
  );
}
```

### 示例2: 数据统计面板

```tsx
import { GridLayout } from '@/components/common/Layout';
import { StatCard } from '@/components/common/Card';
import { FiTrendingUp, FiFileText, FiUsers } from 'react-icons/fi';

<GridLayout columns={4} gap={6}>
  <StatCard
    title="总创作数"
    value={1234}
    icon={<FiFileText />}
    trend={{ value: 12.5, isPositive: true }}
    description="较上周增长"
    color="purple"
  />
  <StatCard
    title="总阅读量"
    value="45.2K"
    icon={<FiUsers />}
    trend={{ value: 8.3, isPositive: true }}
    color="blue"
  />
  <StatCard
    title="平均互动率"
    value="23.5%"
    icon={<FiTrendingUp />}
    trend={{ value: -2.1, isPositive: false }}
    color="green"
  />
  <StatCard
    title="草稿数量"
    value={45}
    icon={<FiFileText />}
    color="orange"
  />
</GridLayout>
```

### 示例3: 带动画的卡片列表

```tsx
import { motion } from 'framer-motion';
import { ContentCard } from '@/components/common/Card';
import { staggerContainer, staggerItem } from '@/lib/design';

<motion.div
  variants={staggerContainer}
  initial="hidden"
  animate="show"
>
  {items.map((item, index) => (
    <motion.div key={item.id} variants={staggerItem}>
      <ContentCard
        title={item.title}
        description={item.description}
        icon={item.icon}
        variant="elevated"
      />
    </motion.div>
  ))}
</motion.div>
```

---

## 🎯 设计原则

### 1. 一致性
所有组件遵循智能创作中心的设计语言：
- 紫色渐变主色调
- 圆角卡片设计
- 柔和的阴影效果
- 流畅的动画过渡

### 2. 响应式
所有组件都支持响应式布局：
- 移动端优先
- 自动适配不同屏幕
- 触控友好

### 3. 可访问性
- 语义化HTML
- 键盘导航支持
- ARIA标签
- 良好的对比度

### 4. 性能优化
- 使用Framer Motion优化动画
- React.memo避免不必要的重渲染
- 懒加载大型组件

---

## 🔧 TypeScript 支持

所有组件都提供完整的TypeScript类型定义：

```typescript
import type { ContentCardProps, StatCardProps } from '@/components/common/Card';
import type { PageContainerProps, GridLayoutProps } from '@/components/common/Layout';

// 类型安全的组件使用
const cardProps: ContentCardProps = {
  title: "标题",
  description: "描述",
  variant: "elevated",
};
```

---

## 📚 最佳实践

### 1. 组件组合

优先使用组件组合而非创建新组件：

```tsx
// ✅ 好的做法
<PageContainer>
  <Section title="数据统计">
    <GridLayout columns={4}>
      <StatCard {...props1} />
      <StatCard {...props2} />
    </GridLayout>
  </Section>
</PageContainer>

// ❌ 避免这样做
<CustomPageWithStatsBuiltIn />
```

### 2. 动画使用

适度使用动画，避免过度：

```tsx
// ✅ 好的做法 - 入场动画
<motion.div {...cardEntrance(0.2)}>
  内容
</motion.div>

// ❌ 避免 - 过多动画
<motion.div
  animate={{ rotate: [0, 360], scale: [1, 1.5, 1] }}
  transition={{ repeat: Infinity }}
>
  内容（太花哨了）
</motion.div>
```

### 3. 颜色使用

使用设计令牌而非硬编码颜色：

```tsx
// ✅ 好的做法
import { colors } from '@/lib/design';
<div className={`bg-gradient-to-r ${colors.gradients.purple}`}>

// ❌ 避免这样做
<div className="bg-gradient-to-r from-purple-400 to-pink-500">
```

---

## 🐛 常见问题

### Q: 如何自定义卡片样式?

使用 `className` prop 添加额外样式：

```tsx
<ContentCard
  title="标题"
  className="border-2 border-purple-500"
/>
```

### Q: 如何禁用动画?

设置 `hoverEffect={false}` 或不使用 motion 组件。

### Q: 如何适配暗色模式?

目前组件不支持暗色模式，这是未来的功能规划。

---

## 🔄 版本历史

### v1.0.0 (2025-01-13)

初始版本发布:
- ✅ BaseCard, ContentCard, StatCard
- ✅ PageContainer, GridLayout, Section
- ✅ 设计令牌系统
- ✅ 动画工具库
- ✅ 完整TypeScript支持

---

## 📞 反馈与支持

如有问题或建议，欢迎反馈：
- 📧 Email: support@content-factory.com
- 💬 微信: ContentFactory
- 🐛 Bug反馈: GitHub Issues

---

**祝你使用愉快！🎉**

*最后更新时间: 2025-01-13*
